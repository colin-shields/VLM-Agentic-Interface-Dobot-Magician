# Core application dependencies
# Install with:  pip install -r requirements.txt

# Gemini AI (new unified SDK — replaces google-generativeai)
google-genai>=1.5.0

# Web UI
streamlit>=1.41.0

# Computer vision & image processing
opencv-python>=4.10.0
Pillow>=11.0.0

# Numerical computing (required by OpenCV homography)
numpy>=2.0.0

# Environment variable management (required for .env support)
python-dotenv>=1.0.1
